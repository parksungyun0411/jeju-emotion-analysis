import pandas as pd
from datetime import datetime

# TF-IDF 벡터화 원리 설명서 생성
report = f"""🎯 TF-IDF 벡터화 원리 및 실험 과정 상세 설명서
============================================================

📊 작성 정보:
  - 작성일: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}
  - 목적: 한국어 제주어 감정 분류를 위한 TF-IDF 벡터화 과정 설명
  - 데이터: 최종데이터.xlsx (72,879개), 최종데이터_균형.xlsx (12,950개)

🔤 1. TF-IDF 벡터화란?
============================================================

📋 기본 개념:
- TF-IDF = Term Frequency-Inverse Document Frequency
- 텍스트를 숫자 벡터로 변환하는 기법
- 단어의 중요도를 수치화하여 문서를 벡터로 표현

🔧 구성 요소:

1. **TF (Term Frequency) - 단어 빈도**
   - 특정 문서에서 특정 단어가 나타나는 빈도
   - 공식: TF(t,d) = (단어 t가 문서 d에서 나타나는 횟수) / (문서 d의 총 단어 수)

2. **IDF (Inverse Document Frequency) - 역문서 빈도**
   - 전체 문서 집합에서 특정 단어가 나타나는 문서의 빈도
   - 공식: IDF(t,D) = log(전체 문서 수 / 단어 t가 나타나는 문서 수)
   - 희귀한 단어일수록 높은 값

3. **TF-IDF = TF × IDF**
   - 단어의 중요도를 종합적으로 평가
   - 자주 나타나지만 특정 문서에만 나타나는 단어가 높은 점수

🔤 2. 우리 실험에서의 TF-IDF 설정
============================================================

🔧 TfidfVectorizer 파라미터:
```python
TfidfVectorizer(
    max_features=30000,        # 최대 30,000개 특성(단어) 사용
    ngram_range=(1, 3),        # 1-gram, 2-gram, 3-gram 사용
    stop_words=None,           # 불용어 제거 안함 (한국어는 별도 처리 필요)
    lowercase=False,           # 대소문자 구분 안함 (한국어 특성)
    sublinear_tf=True,         # sublinear TF 스케일링 적용
    use_idf=True,              # IDF 사용
    smooth_idf=True            # IDF 스무딩 적용
)
```

📊 N-gram 설명:
- **1-gram (단어)**: "나는", "행복하다", "오늘"
- **2-gram (단어 쌍)**: "나는 행복하다", "행복하다 오늘"
- **3-gram (단어 3개)**: "나는 행복하다 오늘"

🔤 3. 벡터화 과정 상세
============================================================

🔧 단계별 과정:

**1단계: 텍스트 전처리**
```python
def preprocess_text(self, text):
    # 특수문자 제거 (문장 부호는 유지)
    text = re.sub(r'[^\w\s.!?]', ' ', text)
    # 연속된 공백 제거
    text = re.sub(r'\s+', ' ', text)
    return text.strip()
```

**2단계: TF-IDF 벡터화**
```python
# 예시: 3개 문서
documents = [
    "나는 오늘 행복하다",
    "오늘 날씨가 좋다", 
    "나는 좋은 하루를 보낸다"
]

# 1-gram 결과 (단어별)
# "나는": [1, 0, 1]  # 문서1,3에 나타남
# "오늘": [1, 1, 0]  # 문서1,2에 나타남
# "행복하다": [1, 0, 0]  # 문서1에만 나타남
# "날씨가": [0, 1, 0]  # 문서2에만 나타남
# "좋다": [0, 1, 0]  # 문서2에만 나타남
# "좋은": [0, 0, 1]  # 문서3에만 나타남
# "하루를": [0, 0, 1]  # 문서3에만 나타남
# "보낸다": [0, 0, 1]  # 문서3에만 나타남
```

**3단계: TF 계산**
```
문서1 "나는 오늘 행복하다":
- "나는": 1/3 = 0.33
- "오늘": 1/3 = 0.33  
- "행복하다": 1/3 = 0.33

문서2 "오늘 날씨가 좋다":
- "오늘": 1/3 = 0.33
- "날씨가": 1/3 = 0.33
- "좋다": 1/3 = 0.33
```

**4단계: IDF 계산**
```
전체 문서 수: 3
- "나는": 2개 문서에 나타남 → IDF = log(3/2) = 0.18
- "오늘": 2개 문서에 나타남 → IDF = log(3/2) = 0.18
- "행복하다": 1개 문서에 나타남 → IDF = log(3/1) = 0.48
- "날씨가": 1개 문서에 나타남 → IDF = log(3/1) = 0.48
```

**5단계: TF-IDF 계산**
```
문서1의 "행복하다":
TF = 0.33, IDF = 0.48
TF-IDF = 0.33 × 0.48 = 0.16

문서1의 "나는":
TF = 0.33, IDF = 0.18  
TF-IDF = 0.33 × 0.18 = 0.06
```

🔤 4. 우리 실험에서의 실제 벡터화
============================================================

📊 데이터 규모:
- **최종데이터.xlsx**: 72,879개 문서
- **최종데이터_균형.xlsx**: 12,950개 문서
- **벡터 차원**: 30,000개 특성 (단어/구문)

🔧 실제 벡터화 과정:

**1단계: 표준어 문장 추출**
```python
# 예시 데이터
표준어_문장 = [
    "아 진짜? 그거 왜 언니 안보내줬어?",
    "그래서 세일 할거래 오픈기념으로",
    "하루 당일치기로 갔다 올까 하긴 했는데"
]
```

**2단계: N-gram 생성**
```
1-gram: ["아", "진짜", "그거", "왜", "언니", "안보내줬어"]
2-gram: ["아 진짜", "진짜 그거", "그거 왜", "왜 언니", "언니 안보내줬어"]
3-gram: ["아 진짜 그거", "진짜 그거 왜", "그거 왜 언니", "왜 언니 안보내줬어"]
```

**3단계: TF-IDF 벡터 생성**
```python
# 결과: 각 문서가 30,000차원 벡터로 변환
문서1_벡터 = [0.12, 0.08, 0.15, 0.03, ...]  # 30,000개 값
문서2_벡터 = [0.05, 0.11, 0.07, 0.09, ...]  # 30,000개 값
문서3_벡터 = [0.09, 0.06, 0.13, 0.04, ...]  # 30,000개 값
```

🔤 5. 벡터화 후 머신러닝 모델 적용
============================================================

🔧 모델별 벡터 활용:

**1. Naive Bayes**
```python
# 각 클래스별로 단어 확률 계산
P(단어|감정) = (감정 클래스에서 단어 출현 횟수 + 1) / (감정 클래스 총 단어 수 + 어휘 크기)
```

**2. Logistic Regression**
```python
# 선형 결합: z = w₀ + w₁×벡터₁ + w₂×벡터₂ + ... + w₃₀₀₀₀×벡터₃₀₀₀₀
# 시그모이드: P(감정) = 1 / (1 + e^(-z))
```

**3. SVM**
```python
# 고차원 공간에서 마진 최대화
# 서포트 벡터: 경계에 가장 가까운 데이터 포인트들
```

**4. Random Forest**
```python
# 각 나무마다 랜덤하게 특성 선택
# 30,000개 특성 중 일부만 사용하여 의사결정나무 생성
```

🔤 6. TF-IDF의 장단점
============================================================

✅ 장점:
1. **단어 중요도 반영**: 자주 나타나지만 특정 문서에만 나타나는 단어에 높은 가중치
2. **문서 길이 정규화**: 문서 길이에 관계없이 일관된 벡터 크기
3. **희소성**: 대부분의 값이 0인 희소 행렬로 메모리 효율적
4. **해석 가능성**: 각 차원이 특정 단어/구문을 의미

❌ 단점:
1. **순서 정보 손실**: 단어의 순서나 위치 정보를 고려하지 않음
2. **의미 정보 부족**: 단어의 의미나 문맥을 고려하지 않음
3. **고차원성**: 단어 수만큼 차원이 증가하여 차원의 저주 문제
4. **희소성**: 대부분의 값이 0이어서 정보 손실 가능

🔤 7. 우리 실험에서의 TF-IDF 결과
============================================================

📊 벡터화 결과:
- **최종데이터.xlsx**: (72,879 × 30,000) 희소 행렬
- **최종데이터_균형.xlsx**: (12,950 × 30,000) 희소 행렬
- **메모리 사용량**: 약 수 GB (희소 행렬로 압축)

🔧 특성 분석:
- **1-gram**: 개별 단어 (예: "행복", "슬픔", "화나")
- **2-gram**: 단어 쌍 (예: "정말 행복", "너무 슬픔")
- **3-gram**: 단어 3개 (예: "정말 너무 행복", "오늘 정말 슬픔")

📊 성능에 미치는 영향:
- **고차원성**: 30,000차원으로 인한 차원의 저주
- **희소성**: 대부분의 값이 0이어서 정보 손실
- **순서 정보 부족**: 단어 순서를 고려하지 않아 문맥 정보 손실

🔤 8. 다른 벡터화 방법과의 비교
============================================================

🔧 대안적 방법들:

**1. Word2Vec**
- 단어를 밀집 벡터로 표현
- 의미적 유사성 반영
- 순서 정보 일부 보존

**2. BERT**
- 문맥을 고려한 단어 표현
- 양방향 언어 모델
- 최신 딥러닝 기법

**3. Count Vectorizer**
- 단순 단어 빈도 계산
- TF-IDF보다 단순하지만 성능 낮음

**4. N-gram만 사용**
- 단어 순서 정보 보존
- 하지만 여전히 의미 정보 부족

🔤 9. 결론 및 향후 개선 방향
============================================================

🎯 현재 TF-IDF의 한계:
1. **문맥 정보 부족**: 단어의 의미나 문맥을 고려하지 않음
2. **순서 정보 손실**: 단어의 순서나 위치 정보를 고려하지 않음
3. **고차원성**: 30,000차원으로 인한 차원의 저주
4. **희소성**: 대부분의 값이 0이어서 정보 손실

🚀 향후 개선 방향:
1. **Word2Vec 도입**: 단어의 의미적 유사성 반영
2. **BERT 활용**: 문맥을 고려한 단어 표현
3. **특성 선택**: 중요하지 않은 특성 제거
4. **차원 축소**: PCA, LDA 등으로 차원 감소
5. **앙상블**: 여러 벡터화 방법 결합

📁 관련 파일들:
- 다중감정분류_모델.py (TF-IDF 벡터화 코드)
- models/ (학습된 모델들)
- results/ (성능 결과)
- 베이스라인_성능결과_통합.txt

📊 실험 결과 요약:
- TF-IDF 기반 4개 모델 모두 30-50% 정확도 달성
- N-gram 기반 전통적 머신러닝의 한계 확인
- 더 정확한 성능을 위해서는 딥러닝 모델 필요
"""

# 보고서 저장
with open('TF_IDF_벡터화_원리_설명서.txt', 'w', encoding='utf-8') as f:
    f.write(report)

print("✅ TF-IDF 벡터화 원리 설명서가 생성되었습니다!")
print("📁 파일명: TF_IDF_벡터화_원리_설명서.txt")
print("\n" + "="*60)
print(report)
